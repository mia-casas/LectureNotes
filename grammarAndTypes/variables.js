/* Variables
-Named containers for storing data values
-We name the variables so that we can refer to them and the values they hold at a later time
- Best practice! Define var using "let" or "const"
*/

let a = 2;


/*
(1)...(2)..(3)..(4)
let....b....=....2

1. JS keyword to denote the creation of a variable
2. The name of the var, in which the developer refers to in order to grab the value stored
3. Assignment operator
4. Initial value
*/

// The declaration is to the left of the assignment operator
// This is where the variable is created by utilizing a designated JS keyword(var, let, const) and giving it a name

let s;

// The initalizatioln is to the right of the assignment operator
// This is where the developer sets the value for declared variable

let x = 10;


let y;
console.log("Declaration:", y);

y=10;
console.log("Initialization 1:", y);

y=35;
console.log("Initialization 2:", y);