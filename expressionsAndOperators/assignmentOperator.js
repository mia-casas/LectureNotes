 // The assignment operator is used to assign values using the initialization (right hand side of the =) to supply the declaration (left side of th +) a value).

 let r = 5;
 // = is assignment operator